$("#main_header").html("<%= escape_javascript(render 'header', locals: { groups: @groups} ) %>" );
$('<%= escape_javascript( if is_admin? then "#main_add" else "#nil" end ) %>').html("<%= escape_javascript(render partial: 'form', locals: { group: Group.new } ) %>" );
$("#main_content").html("<%= escape_javascript(render partial: 'index', locals: { groups: @groups} ) %>" );
