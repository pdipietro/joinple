if($("#groups-page").length == 0) {
  $("#tech-info").html("<%= escape_javascript(render partial: 'sessions/tech_info') %>" );
  $("#main-header").html("<%= escape_javascript(render partial: 'header', locals: { group: Group.new} ) %>" );
  $("#main-add").html("");
  $("#main-content").html("<%= escape_javascript(render partial: 'index', locals: { groups: @groups, subset: subset, title: title } ) %>" );
}
else
 $("#main-content").html("<%= escape_javascript(render partial: 'index', locals: { groups: @groups, subset: subset, title: title } ) %>" );
