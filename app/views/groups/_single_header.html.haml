- if is_admin?
  -#.p
    - user = current_user
    - csn = current_social_network
    - search = PostComment.new

  #post-header
  = image_tag group.header.url(:xl_banner), :class => "responsive"
  #common-header-left
    .buttons.nothing
      .post-icon.nothing
        = link_to dummy_path, :remote => :true, :title => "View discussions" do
          %i.activable.im-all-posts.im-2x
      .post-icon.nothing
        = link_to dummy_path, :remote => :true, :title => "View group members" do
          %i.activable.im-all-posts.im-2x
      .post-icon.nothing
        = link_to dummy_path, :remote => :true, :title => "View group admin" do
          %i.activable.im-all-posts.im-2x
      = render partial: 'shared/search'
  .spacer
 