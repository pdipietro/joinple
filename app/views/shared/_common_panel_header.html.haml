-#****************************************************************************************************************
  - sample: render partial: 'shared/common_panel_heading', locals: { :style => "your style", :data_target => "your target", :title => "your title"}
  - Needs three params:
    - style: a String usually one of
      - "current" for current_social_network_style
      - "super" super_social_network_style
      - or a different style that will be passed as a block
    - data-target: the toggler to be used 
      - example: "#group-form"
    - title
  ****************************************************************************************************************
- debugger
- title = defined?(title) ? title : "Add"
- decoded_style = case style                              |
    when "current"  then current_social_network_style     | 
    when "super"    then super_social_network_style       |
    else                 style                            |
  end                                   
.panel-heading{ :style => decoded_style }
  .float-right{ :style => "padding-top: 10px; padding-right: 10px; "}
    = link_to hide_path("#{uuid}"), :remote => :true, :title => t("button.exit") do
      .panel-title
        - if defined?(title)
          = title
        - else
          - raise "TITLE MISSED"
        %i.im-uncheck.to-right{ :style => "color: #ffffff;"}
