- if logged_in?
  - unless defined?(style)
    - csn = current_social_network
    - style = current_social_network_style
  #social-network{ :style => style }
    = cl_image_tag csn.logo, :name => "social_network#{csn.uuid}", :class => "social-network-logo"
