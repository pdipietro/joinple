$("#main_header").html("<%= escape_javascript(render partial: "'#{class_name.downcase}/header'", locals: { "'#{class_name.downcase.pluralize}'": '@"#{class_name.downcase.pluralize}"''} ) %>" );
$('<%= escape_javascript( if is_admin? then "#main_add" else "#nil" end ) %>').html("<%= escape_javascript(render partial: 'form', locals: { language: Language.new } ) %>" );
$("#main_content").html("<%= escape_javascript(render partial: 'index', locals: { languages: @languages} ) %>" );
