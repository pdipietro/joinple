$("#main_add").html("<%= escape_javascript(render partial: 'discussion/form', locals: { discussion: @discussion, title: 'Add a new discussion' } ) %>" );
$("#discussion-form").removeClass('in').addClass('in');
