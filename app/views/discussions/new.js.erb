$("#main-add").html("<%= escape_javascript(render partial: 'discussions/form', locals: { discussion: @discussion, title: 'Add a new discussion' } ) %>" );
$("#discussion-form").removeClass('in').addClass('in');
