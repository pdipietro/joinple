if($("#discussion-page").length == 0) {
  $("#tech-info").html("<%= escape_javascript(render partial: 'sessions/tech_info', locals: { view: 'discussion::list.js.erb (1)' }) %>" );
  $("#main-header").html("<%= escape_javascript(render partial: 'discussions/header', locals: { discussion: Discussion.new} ) %>" );
  $("#main-add").html("");
  $("#main-content").html("<%= escape_javascript(render partial: 'discussions/index', locals: { discussions: @result, subset: subset, title: title, show_group: show_group, show_social: show_social } ) %>" );
} 
else {
 $("#main-content").html("<%= escape_javascript(render partial: 'discussions/index', locals: { discussions: @result, subset: subset, title: title, show_group: show_group, show_social: show_social} ) %>" );
 $("#tech-info").html("<%= escape_javascript(render partial: 'sessions/tech_info', locals: { view: 'discussion::list.js.erb (2)' }) %>" );
}