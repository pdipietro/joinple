if($("#discussion-page").length == 0) {
  $("#tech-info").html("<%= escape_javascript(render partial: 'sessions/tech_info') %>" );
  $("#main-header").html("<%= escape_javascript(render partial: 'header', locals: { discussion: Discussion.new} ) %>" );
  $("#main-add").html("");
  $("#main-content").html("<%= escape_javascript(render partial: 'index', locals: { discussions: @discussions, subset: subset, title: title } ) %>" );
}
else
 $("#main-content").html("<%= escape_javascript(render partial: 'index', locals: { discussions: @discussions, subset: subset, title: title } ) %>" );
