$("#main_header").html("<%= escape_javascript(render partial: 'header', locals: { posts: @posts} ) %>" );
$('<%= escape_javascript( if is_admin? then "#main_add" else "#nil" end ) %>').html("<%= escape_javascript(render partial: 'form', locals: { post: Post.new } ) %>" );
$("#main_content").html("<%= escape_javascript(render partial: 'index', locals: { posts: @posts} ) %>" );
