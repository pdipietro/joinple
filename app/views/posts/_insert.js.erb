$("#main_add").html("<%= escape_javascript(render partial: 'form', locals: { post: Post.new, current_owner: current_owner } ) %>" );
$("#main_content").children().before("<%= escape_javascript(render partial: 'show', locals: { post: @post, current_owner: current_owner } ) %>" );
