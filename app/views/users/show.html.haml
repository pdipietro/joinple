- provide(:title, @user.full_name)
.row
  %aside.col-md-4
    %section.user_info
      %h1
        = gravatar_for @user
        = @user.full_name
      %h3
        = "( #{@user.nickname})"
  .col-md-8
    - posts = Post.find_by @user
    - posts.each do |post|
      %ol
        = render @post
