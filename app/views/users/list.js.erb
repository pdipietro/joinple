if($("#users-page").length == 0) {
  $("#tech_info").html("<%= escape_javascript(render partial: 'sessions/tech_info') %>" );
  $("#main_header").html("<%= escape_javascript(render partial: 'header', locals: { user: Users.new} ) %>" );
  $("#main_add").html("");
  $("#main_content").html("<%= escape_javascript(render partial: 'index', locals: { users: @users, subset: subset, title: title } ) %>" );
}
else
 $("#main_content").html("<%= escape_javascript(render partial: 'index', locals: { users: @users, subset: subset, title: title } ) %>" );
