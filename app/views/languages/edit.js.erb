$("#main-add").html("<%= escape_javascript(render partial: 'form', locals: { language: @language, title: "Edit the #{@language.code} language" } ) %>" );
$("#language-form").removeClass('in').addClass('in');
