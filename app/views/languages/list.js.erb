if($("#lists-page").length == 0) {
  $("#tech-info").html("<%= escape_javascript(render partial: 'sessions/tech_info') %>" );
  $("#main-header").html("<%= escape_javascript(render partial: 'header', locals: { language: Language.new} ) %>" );
  $("#main-add").html("");
  $("#main-content").html("<%= escape_javascript(render partial: 'index', locals: { languages: @languages, title: ""} ) %>" );
  $("#main-content").html("<%= escape_javascript(render partial: 'index', locals: { languages: @languages, subset: subset, title: title } ) %>" );
  $("#main-right").html("<%= escape_javascript(render partial: 'rindex', locals: { languages: @languages } ) %>" );
}
else
 $("#main-content").html("<%= escape_javascript(render partial: 'index', locals: { languages: @languages, subset: subset, title: title } ) %>" );
