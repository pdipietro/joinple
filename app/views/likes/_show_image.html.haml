= link_to hide_image_path, :remote => :true, :title => "Close" do
  .full_screen_image
  %img.image-middle-transparent.responsive{:src => UrlSafeBase64.decode64(image), :alt => ""}
  %img.image-middle.shadowing.responsive{:src => UrlSafeBase64.decode64(image), :alt => ""}
