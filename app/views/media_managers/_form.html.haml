= form_for @media_manager do |f|
  - if @media_manager.errors.any?
    #error_explanation
      %h2= "#{pluralize(@media_manager.errors.count, "error")} prohibited this media_manager from being saved:"
      %ul
        - @media_manager.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
