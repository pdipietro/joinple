$('#<%= escape_javascript(@pdiscussion-comment.uuid) %>').html("<%= escape_javascript(render partial: 'edit', locals: { discussion_comment: @discussion-comment, title: "Edit discussion comment"  } ) %>" );