$('#<%= escape_javascript(@discussion_comment.uuid) %>').html("<%= escape_javascript(render partial: 'edit', locals: { discussion_comment: @discussion_comment, discussion: discussion, title: "Edit discussion comment"  } ) %>" );