$('#<%= escape_javascript(@discussion_comment.uuid)%>').html("<%= escape_javascript(render partial: 'show', locals: { discussion_comment: @discussion_comment, parent_uuid: parent_uuid, parent_class: parent_class } ) %>" );
