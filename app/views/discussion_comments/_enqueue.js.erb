$('#<%= escape_javascript(post.uuid)%>').find(' > div:nth-last-child(2)').before("<%= escape_javascript(render partial: 'show', locals: { discussion_comment: @discussion_comment, discussion: discussion } ) %>" );
$('#<%= escape_javascript(post.uuid)%>').find(' textarea').val("");