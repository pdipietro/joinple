$("#main_add").html("<%= escape_javascript(render partial: 'form', locals: { discussion_comment: discussion_comment, discussion: discussion } ) %>" );
$("#group-discussion").removeClass('in').addClass('in');
